// cms/src/api/source-website/controllers/run-now.ts

// If you want a typed Koa Context, you can:
// import type { Context } from 'koa';

/*export default ({ strapi: _strapi }) => ({
// async run(ctx: Context) {
async run(ctx: any) {
    // Example: you could trigger a queue/cron here later using _strapi
    ctx.send({ ok: true });
},
}); */
